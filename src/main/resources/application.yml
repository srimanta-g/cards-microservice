spring:
  application:
    name: cards
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: root
    password: root
    database-platform: org.hibernate.dialect.H2Dialect
    console:
      enabled:true
  profiles:
    active: default

server:
  port: 8081

custom:
  env: default
  info:
    name: Cards Service
    description: A service for managing cards
    version: 1.0.0
    contact:
      name: Srimanta Ganguly
      email: srimantagangulu02@gamil.com